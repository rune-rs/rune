fn foo(n, u) {
    // trying to replace a n with a pointer that is outside of the
    // current stack frame.
    let v = 5;
    *n = u;
}

fn main() {
    let u = 5;
    let v = 0;
    let n = &v;
    foo(n, &u);
    n
}