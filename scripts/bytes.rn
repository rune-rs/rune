use bytes;
use test::assert;

fn main() {
    let bytes = bytes::Bytes::new();
    bytes.extend_str("hello world");
    bytes.extend_str("hello world");
    let other = bytes.clone();
    bytes.extend(other);

    assert(bytes is bytes::Bytes, "bytes should be bytes");
    dbg(bytes);

    bytes
}