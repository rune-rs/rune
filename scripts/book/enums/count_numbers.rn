fn count_numbers(limit) {
    let limit = limit.unwrap_or(10);

    for n in 0..limit {
        println!("Count: {}", n);
    }
}

println!("First count!");
count_numbers(None);

println!("Second count!");
count_numbers(Some(2));
